我建议分 4 步走，不要一下子做大。

## 第 1 步：把 toy 后端理解透  
你现在正在做这一步。重点是搞懂：

```text
HttpRequest 是什么
HttpResponse 是什么
Router 怎么分发
Handler 为什么能保存处理函数
application.cpp 为什么负责注册路由
simple_server.cpp 为什么负责收请求和发响应
```

这一阶段的目标不是复杂，而是你能讲清楚：

```text
浏览器发请求后，我的 C++ 程序内部发生了什么？
```

## 第 2 步：从固定响应升级成真实接口  
比如新增一个接口：

```text
POST /api/v1/chat
```

请求：

```JSON
{
  "message": "你好"
}
```

返回：

```JSON
{
  "code": "OK",
  "data": {
    "reply": "我收到了：你好"
  }
}
```

这一步会引入：

```text
读取请求 body
解析 JSON
参数校验
统一错误码
统一响应格式
```

这时它就不只是 `/health` 这种固定接口了。

## 第 3 步：迁移到 Drogon 产品版  
现在手写 HTTP Server 是教学版，适合学习原理，但不适合长期做产品。产品版应该用 Drogon，因为它会帮你处理：

```text
成熟路由
JSON
并发
异常处理
请求日志
文件上传
SSE / WebSocket
数据库插件
Redis
```

所以后续主线应该是：

```
保留 toy 版用于讲原理
新增 Drogon 版用于做真正项目
```

## 第 4 步：接真实业务模块  
等 Drogon 骨架跑通后，再按顺序做：

```text
1. 统一响应和错误码
2. MySQL / Redis 配置
3. 用户登录和权限
4. 文档上传
5. 文档切分和入库
6. Embedding 和向量库
7. RAG 问答
8. SSE 流式返回
9. Tool Calling 查询业务流程
```

你现在的位置可以这样理解：

```text
现在：
浏览器 -> C++ 后端 -> Router -> 固定 JSON

下一阶段：
浏览器 -> C++ 后端 -> Router -> 解析请求 -> 业务函数 -> JSON 响应

产品阶段：
浏览器 -> Drogon 后端 -> Controller -> Service -> MySQL/Redis/AI模型 -> 统一响应
```

所以短期最正确的下一步是：**先把 toy 后端启动起来，确认 `/health` 能访问；然后新增一个简单的 `POST /api/v1/chat`，让它接收用户输入并返回 JSON。**这一步最适合你现在的学习阶段。

## 1.1
最该理解的是这条链路：

```
浏览器/ curl 发 JSON
  ↓
HTTP body 里放着 JSON 字符串
  ↓
后端从 request.body 读取
  ↓
解析出 message
  ↓
业务逻辑生成 reply
  ↓
返回 JSON
```
真正理解：

```
POST body 是什么
Content-Type 是什么
参数为什么会缺失
为什么缺参数要返回 400
为什么返回结构要统一
```


## 1.2 application中加POST相关的函数
1. 修复返回 JSON 拼接 bug

2. 给 /api/v1/chat 补测试

3. 让 extract_message 支持冒号前后有空格

4. 用 curl 验证正常请求和错误请求

## 2 接入JSON库

先确认当前 Makefile、测试和实现的最新内容，然后只改这三处：
third_party/nlohmann/json.hpp、Makefile include 路径、application.cpp 解析逻辑，
再用现有测试和新增测试验证。

 nlohmann/json 最新发布版是 v3.12.0，现在下载它的单头文件到 third_party/nlohmann/json.hpp。

以后你每次改项目，直接跑：

```PowerShell
cd "e:\note\面试八股 1\6_AI应用开发\20_Cpp企业AI_Copilot项目\code\cpp-ai-copilot"
mingw32-make clean
mingw32-make test
mingw32-make all
```
[[第三阶段接入JSON库]]

对，就是这两件事。我已经做完了：

```
好滴，开始做第三阶段吧，现在只需要修改application.cpp让完全走json库，然后补充一些出错的输出对吧
```
在 [application.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/application.cpp) 里：

- 删除了 `__has_include` 和手写 fallback 解析逻辑。
- 现在强制使用 `nlohmann/json.hpp`。
- `extract_message()` 用 `nlohmann::json::parse(request.body)` 解析请求 body。
- 成功响应用 JSON 库生成。
- 错误响应也用 JSON 库生成，不再手写拼字符串。

在 [test_core.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/tests/test_core.cpp) 里补了错误输入测试：

```
message 缺失 -> 400
message 不是字符串 -> 400
message 是空字符串 -> 400
JSON 格式错误 -> 400
message 里有转义引号 -> 200
```

验证已通过：

```
mingw32-make test 通过
mingw32-make all 通过
```

现在第 3 阶段已经更清楚了：`POST /api/v1/chat` 的请求解析、参数校验、成功响应和错误响应都已经由 JSON 库支撑。下一步就可以开始第 4 阶段：把 `application.cpp` 里的聊天逻辑拆到 `chat_service`、统一响应拆到 `api_response`。

## 第四阶段
[[11_整体项目的运行流程和大方向的把控^]]
第 4 阶段：重构代码结构application.cpp 
只注册路由chat_service.cpp 
处理聊天业务response.hpp 
统一响应格式error_code.hpp 
管理错误码

第四阶段的验收标准可以定成这几条：

```
目标：application.cpp 不再堆业务细节，只负责装配路由。
```

具体验收标准：

```
1. application.cpp 只做三件事：
   - 创建 Router
   - 注册 /health、/api/v1/ping、/api/v1/chat
   - 返回 Router
```

```
2. chat 相关逻辑从 application.cpp 拆出去：
   - 解析 request.body
   - 校验 message
   - 生成 reply
   - 返回 400 / 200
```

```
3. 统一响应从 application.cpp 拆出去：
   - 成功响应：{"code":"OK","data":...}
   - 错误响应：{"code":"INVALID_REQUEST","message":"...","data":null}
```

```
4. 错误码集中管理：
   - OK
   - INVALID_REQUEST
   - 后续可以扩展 ROUTE_NOT_FOUND、INTERNAL_ERROR
```

```
5. 原有接口行为不变：
   - GET /health 仍然返回 200
   - GET /api/v1/ping 仍然返回 pong
   - POST /api/v1/chat 正常 message 返回 200
   - 缺少 message 返回 400
   - message 不是字符串返回 400
   - message 为空返回 400
   - JSON 格式错误返回 400
```

```
6. 测试必须全部通过：
   mingw32-make test
```

```
7. 服务端仍然能编译：
   mingw32-make all
```

最终你应该能看着代码说清楚：

```
application.cpp 是路由装配层
chat_service 是业务处理层
api_response 是响应格式层
error_code 是错误定义层
```

这就是从 toy 项目往真实后端项目走的关键一步。

## 第五阶段
可以开始第五阶段。先把边界定清楚：**第五阶段不是把所有业务都迁过去，而是先做 Drogon 产品版骨架。**

它的目标是：

```
保留 toy server 作为教学版
新增 Drogon server 作为产品版
先跑通 /health、统一响应、错误处理、日志、配置
```

暂时不做：

```
MySQL
Redis
SSE
RAG
文档上传
登录鉴权
```

**第五阶段验收标准**

```
1. Drogon 源码或依赖获取成功
```

要能明确记录：

```
Drogon 版本
下载来源
构建方式
是否需要 Trantor 子模块
```

我刚查了一下官方 GitHub latest 页面，当前显示 latest release 是 `v1.9.8`，所以我们不能盲目沿用旧计划里的 `v1.9.13`，要以实际官方 release/tag 为准。来源：[https://github.com/drogonframework/drogon/releases/latest](https://github.com/drogonframework/drogon/releases/latest)

```
2. toy 版不能被破坏
```

这两个必须继续通过：

```
mingw32-make test
mingw32-make all
```

```
3. 新增产品版构建目标
```

比如最后能有：

```
cpp-ai-copilot.exe              toy 教学版
cpp-ai-copilot-product.exe      Drogon 产品版
```

```
4. Drogon 产品版能启动
```

默认监听：

```
127.0.0.1:18081
```

避免和 toy 版的：

```
127.0.0.1:18080
```

冲突。

```
5. 产品版 GET /health 能访问
```

返回统一 JSON：

```
{
  "code": "OK",
  "message": "success",
  "data": {
    "status": "ok",
    "service": "cpp-ai-copilot",
    "version": "0.2.0"
  }
}
```

```
6. 产品版错误响应统一
```

至少验证：

```
GET /missing      -> 404 + ROUTE_NOT_FOUND
POST /health      -> 405 + METHOD_NOT_ALLOWED
内部异常          -> 500 + INTERNAL_ERROR
```

```
7. 产品版有请求日志
```

至少记录：

```
请求方法
请求路径
状态码
耗时
```

类似：

```
GET /health -> 200 in 1.2ms
GET /missing -> 404 in 0.8ms
```

```
8. CMake 能构建产品版
```

后续成熟 C++ 项目要靠 CMake，而不是只靠 Makefile。

验收命令类似：

```
cmake -S . -B build-product -G Ninja
cmake --build build-product
ctest --test-dir build-product --output-on-failure
```

```
9. README 或过程文档记录清楚
```

要写明：

```
为什么从 toy server 迁到 Drogon
Drogon 负责什么
toy 版和产品版区别是什么
怎么构建
怎么启动
怎么测试
```

**做完之后你要能这样解释**

```
我第一版先手写了一个最小 HTTP Server，用来理解 HTTP 请求从 socket 进入、解析、路由分发、生成响应、返回客户端的全过程。

但手写版适合学习，不适合做完整产品。真实后端需要成熟的路由、并发、异常处理、JSON、日志、文件上传、数据库、Redis、SSE 等能力，所以我保留手写版作为教学版，同时新增 Drogon 产品版。

Drogon 产品版第一步只实现 /health、统一响应、统一错误处理、配置加载和请求日志。这样后续 MySQL、Redis、文档上传、RAG、SSE 都可以在成熟框架上继续扩展。
```

**第五阶段执行顺序**

```
1. 确认 Drogon 官方可用版本
2. 获取 Drogon 源码或安装依赖
3. 改 CMakeLists.txt，加入产品版构建目标
4. 新建 src/product/main.cpp
5. 新建 product 版 api_response / error_code
6. 实现 GET /health
7. 实现 404 / 405 / 500 统一错误响应
8. 加请求日志
9. 跑测试和手动 HTTP 验证
10. 写第五阶段过程文档
```

如果你确认，我下一步就从 **“确认并获取 Drogon 版本”** 开始做。

### 先不处理Drogon
不要继续处理 Drogon。现在先把 root CMakeLists.txt 里的 Drogon 相关 add_subdirectory 或链接逻辑放到 option(COPILOT_BUILD_DROGON OFF) 后面，默认不构建 Drogon。test_api_response 只依赖 include、src/product，不要链接 Drogon。目标是让产品版 API TDD 测试先能进入编译阶段。
已按你的方向收住了。

我改了 [CMakeLists.txt](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/CMakeLists.txt)：

- 新增 `option(COPILOT_BUILD_DROGON "Build Drogon-based product server" OFF)`
- `BUILD_CTL / BUILD_EXAMPLES / BUILD_ORM / BUILD_BROTLI / BUILD_YAML_CONFIG / USE_SUBMODULE`
- `add_subdirectory(third_party/drogon-src/drogon-1.9.13)`

这些现在都只在：

```
if(COPILOT_BUILD_DROGON)
    ...
endif()
```

里面执行，默认不会构建 Drogon。

同时改了 [tests/test_api_response.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/tests/test_api_response.cpp)：

- 去掉了 `drogon::k404NotFound` 这类 Drogon 枚举依赖
- 改成普通 HTTP 状态码：`404 / 405 / 500`
- `test_api_response` 现在只加了 `include` 和 `src/product` include 路径，不再链接 `Drogon::Drogon`

验证结果：

```
rg 检查：
只剩 COPILOT_BUILD_DROGON 块里有 add_subdirectory(third_party/drogon...)
没有 Drogon::Drogon
没有 tests 里的 drogon::
```

产品 API 测试已经能进入纯编译红灯：

```
fatal error: copilot/product/api_response.hpp: No such file or directory
```

这正好是下一步 TDD 要补的 `include/copilot/product/api_response.hpp`，不是 Drogon 问题。

旧教学版也没被影响：

```
mingw32-make test
./build/test_core.exe
```

通过。

另外说明一下：我尝试跑 CMake 配置时，它仍然在 `CMakeScratch` 临时文件删除阶段报“拒绝访问”，这是 CMake 自己的编译器探测问题，还没进入项目 target 阶段；但现在从代码层面 Drogon 默认构建已经被关掉了。

### 已继续下一步：先把“产品版 API 错误码层”做出来了，而且没有继续处理 Drogon。

**这次改了什么**

- 新增 [include/copilot/product/error_code.hpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/include/copilot/product/error_code.hpp)  
    定义产品版错误码：  
    `OK / INVALID_REQUEST / ROUTE_NOT_FOUND / METHOD_NOT_ALLOWED / INTERNAL_ERROR`
    
- 新增 [src/product/error_code.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/product/error_code.cpp)  
    实现三类映射：  
    `ErrorCode -> 字符串 code`，比如 `route_not_found -> ROUTE_NOT_FOUND`  
    `ErrorCode -> 默认 message`，比如 `route_not_found -> route not found`  
    `ErrorCode -> HTTP 状态码`，比如 `route_not_found -> 404`
    
- 新增 [include/copilot/product/api_response.hpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/include/copilot/product/api_response.hpp)  
    现在它只作为产品版响应模块入口，先聚合 `error_code.hpp`。真正的 `success_body/error_body` 下一条测试再加。
    
- 新增 [tests/test_api_response.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/tests/test_api_response.cpp)  
    目前只测产品版错误码元数据，不依赖 Drogon。
    
- 修改 [CMakeLists.txt](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/CMakeLists.txt)  
    `test_api_response` 现在只编译：  
    `tests/test_api_response.cpp`  
    `src/product/error_code.cpp`
    

**为什么这样改**  
第五阶段先不要直接上 Drogon。正确顺序是先做一个“框架无关”的产品 API 层：

```
产品错误码
  ↓
统一响应格式
  ↓
健康检查业务数据
  ↓
最后再让 Drogon 调用这些模块
```

这样以后从 toy server 换到 Drogon，不会把业务规则写死在框架里面。

**验证结果**  
我先确认过红灯：

```
fatal error: copilot/product/api_response.hpp: No such file or directory
```

然后补最小实现，再验证：

```
g++ ... tests/test_api_response.cpp src/product/error_code.cpp ...
通过
```

```
.\build\test_api_response.exe
通过
```

旧教学版也没坏：

```
mingw32-make test
通过
```

Drogon 默认依赖也确认没有回来：现在只在 `COPILOT_BUILD_DROGON` 开关块里面出现。下一步就是给产品版响应体加测试：`success_body` 和 `error_body`，统一返回 `code/message/data`。
### 继续做完了这一小步：**产品版统一响应体 `success_body / error_body`**。

这次主要改了 3 个地方：

- [tests/test_api_response.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/tests/test_api_response.cpp)  
    新增两个测试：
    
    - `test_success_body_wraps_data_json()`  
        验证成功响应长这样：
        
        ```
        {"code":"OK","message":"success","data":{"status":"ok"}}
        ```
        
    - `test_error_body_uses_default_message_and_null_data()`  
        验证错误响应长这样：
        
        ```
        {"code":"ROUTE_NOT_FOUND","message":"route not found","data":null}
        ```
        
- [include/copilot/product/api_response.hpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/include/copilot/product/api_response.hpp)  
    新增产品版响应函数声明：
    
    ```
    std::string success_body(const std::string& data_json);
    std::string error_body(ErrorCode code);
    ```
    
- [src/product/api_response.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/product/api_response.cpp)  
    新增实现，用 `ErrorCode` 自动生成统一响应格式。
    

同时 [CMakeLists.txt](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/CMakeLists.txt) 里的 `test_api_response` 增加了：

```
src/product/api_response.cpp
src/product/error_code.cpp
```

**为什么这样做**

这一步是在给未来 Drogon 产品版铺底层能力，但它本身不依赖 Drogon：

```
ErrorCode
  ↓
ApiResponse
  ↓
未来 Drogon Controller 调用 ApiResponse
```

也就是说，以后 Drogon 只是“接 HTTP 请求、返回 HTTP 响应”的框架，真正的业务响应格式由 `copilot::product` 自己控制。

**验证**

我先确认测试红灯是对的：

```
error: 'success_body' is not a member of 'copilot::product'
error: 'error_body' is not a member of 'copilot::product'
```

然后补实现，再验证：

```
g++ ... tests/test_api_response.cpp src/product/api_response.cpp src/product/error_code.cpp ...
通过
```

```
.\build\test_api_response.exe
通过
```

旧 toy 后端测试也没坏：

```
mingw32-make test
通过
```

Drogon 仍然没有默认参与构建，只在 `COPILOT_BUILD_DROGON` 开关块里。下一步可以继续加 `health_data(service, version)`，让 `/health` 的产品版响应数据也先独立于 Drogon 做出来。
### 前三步一起提交了

名称为：`Add product API error codes and response bodies`



### 第五阶段的核心验收标准可以定成一句话：

**在不破坏 toy 教学版的前提下，新增一套 Drogon 产品版后端骨架，并且统一响应、健康检查、错误处理都先工程化。**

按你现在的节奏，我建议第五阶段分成两层验收。

**5A：产品版 API 基础层，不依赖 Drogon**

这部分我们正在做。验收标准：

1. `copilot::product::ErrorCode` 完成  
    包含：
    
    ```
    OK
    INVALID_REQUEST
    ROUTE_NOT_FOUND
    METHOD_NOT_ALLOWED
    INTERNAL_ERROR
    ```
    
2. 能把错误码转换成：
    
    ```
    内部 code 字符串
    默认 message
    HTTP 状态码
    ```
    
3. 统一响应格式完成：
    
    ```
    {
      "code": "OK",
      "message": "success",
      "data": {}
    }
    ```
    
    错误响应：
    
    ```
    {
      "code": "ROUTE_NOT_FOUND",
      "message": "route not found",
      "data": null
    }
    ```
    
4. 新增 `health_data(service, version)`  
    返回：
    
    ```
    {
      "status": "ok",
      "service": "cpp-ai-copilot",
      "version": "0.2.0"
    }
    ```
    
5. `test_api_response` 能独立测试这些产品版 API 逻辑，不链接 Drogon。
    
6. 旧 toy 后端测试继续通过：
    
    ```
    mingw32-make test
    ```
    

**5B：Drogon 产品版骨架**

这部分等 5A 稳了再接。验收标准：

1. `COPILOT_BUILD_DROGON` 默认是 `OFF`，不开时不构建 Drogon。
    
2. 打开时可以构建产品版目标：
    
    ```
    cpp-ai-copilot-product
    ```
    
3. 新增 `config/product.json`，产品版默认监听：
    
    ```
    127.0.0.1:18081
    ```
    
4. Drogon 版本实现：
    
    ```
    GET /health
    ```
    
    返回统一 JSON：
    
    ```
    {
      "code": "OK",
      "message": "success",
      "data": {
        "status": "ok",
        "service": "cpp-ai-copilot",
        "version": "0.2.0"
      }
    }
    ```
    
5. 未知路由返回：
    
    ```
    404 + ROUTE_NOT_FOUND
    ```
    
6. 错误方法返回：
    
    ```
    405 + METHOD_NOT_ALLOWED
    ```
    
7. 服务端异常返回：
    
    ```
    500 + INTERNAL_ERROR
    ```
    
8. 请求日志至少包含：
    
    ```
    method
    path
    status code
    elapsed time
    ```
    
9. toy 教学版仍然可以独立运行，不被 Drogon 改造污染。
    

所以我们接下来就按这个来：**先完成 5A，再接 5B**。下一步就是补 `health_data(service, version)` 的 TDD 测试和实现。

#### 5B

相对当前 `origin/main / HEAD`，最后一次提交是：

```
73645ed Add project documentation and architecture notes
```

当前工作区的改动可以分成 4 类。

**1. 产品版 API 层继续完善**

改了：

- [include/copilot/product/api_response.hpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/include/copilot/product/api_response.hpp)
- [src/product/api_response.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/product/api_response.cpp)
- [tests/test_api_response.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/tests/test_api_response.cpp)

新增了：

```
std::string health_data(const std::string& service_name, const std::string& version);
```

作用是生成 `/health` 的 `data` 部分：

```
{"status":"ok","service":"cpp-ai-copilot","version":"0.2.0"}
```

为什么改：  
先把健康检查的业务数据做成框架无关函数，后面 Drogon Controller 只负责调用它，不把业务 JSON 写死在 Drogon 代码里。

**2. 构建入口整理**

改了：

- [Makefile](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/Makefile)

新增：

```
PRODUCT_API_TEST_SRC := tests/test_api_response.cpp src/product/api_response.cpp src/product/error_code.cpp
product-api-test
```

作用：

```
mingw32-make product-api-test
```

可以单独验证产品版 API 层，不依赖 Drogon。

还改了：

- [CMakeLists.txt](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/CMakeLists.txt)

新增：

```
add_library(copilot_product_api ...)
```

并且 `test_api_response` 改成链接这个库。  
同时在 `COPILOT_BUILD_DROGON` 开关里面新增了 `cpp-ai-copilot-product` 目标。

为什么改：  
产品 API 层以后会被两个地方复用：

```
test_api_response
cpp-ai-copilot-product
```

所以抽成 `copilot_product_api` 更清楚。

**3. 新增 Drogon 产品版骨架**

新增文件：

- [config/product.json](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/config/product.json)
- [src/product/drogon_response.hpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/product/drogon_response.hpp)
- [src/product/health_controller.hpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/product/health_controller.hpp)
- [src/product/health_controller.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/product/health_controller.cpp)
- [src/product/main.cpp](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/code/cpp-ai-copilot/src/product/main.cpp)

它们分别干什么：

- `product.json`：产品版配置，默认监听 `127.0.0.1:18081`。
- `drogon_response.hpp`：把产品 API 的 JSON 字符串包装成 Drogon 的 `HttpResponse`。
- `health_controller.*`：实现 Drogon 版 `GET /health`。
- `src/product/main.cpp`：Drogon 产品版入口，注册：
    - 默认 404
    - 统一错误处理
    - 异常处理
    - 请求日志
    - 启动服务

为什么改：  
这是 5B 的骨架，但仍然放在 `COPILOT_BUILD_DROGON=ON` 后面，默认不会影响 toy 后端。

**4. 文档和生成物**

文档改了：

- [每一步都做了什么.md](e:/note/面试八股 1/6_AI应用开发/20_Cpp企业AI_Copilot项目/03_实现路线/01第一周c++Web框架/每一步都做了什么.md)

追加了大约 350 行，记录了：

- 为什么先暂停 Drogon
- 为什么先做产品 API 层
- 错误码层、统一响应体、验收标准
- 第五阶段 5A / 5B 的拆分

未跟踪生成目录：

```
code/cpp-ai-copilot/build-cmake-default/
cb-default/
```

这两个是 CMake 尝试配置时生成的构建目录，不是业务代码，正常不应该提交。现在 `.gitignore` 只忽略了 `/build/` 和 `/build-product/`，还没忽略这两个名字。

**验证过的结果**

已通过：

```
mingw32-make product-api-test
mingw32-make test
```

也就是说：

```
产品 API 层测试通过
toy 教学版测试通过
```

还没完全验收的是 Drogon 产品版，因为 CMake 配置阶段在当前环境出现过 `CMakeScratch` 临时文件“拒绝访问”的问题。代码骨架已经加了，但 Drogon 构建和 HTTP 联调还没通过最终验收。